CREATE TABLE IF NOT EXISTS buyers (
                                      id SERIAL PRIMARY KEY,
                                      name TEXT NOT NULL,
                                      phone TEXT NOT NULL
);